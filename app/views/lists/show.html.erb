<%= @list.name %>

<div class="container">
  <% @list.movies.each do |movie| %>
    <%= movie.title %>
    <%= movie.overview %>
    <%= movie.poster_url %>
    <%= movie.rating %>
  <% end %>
</div>

<%= link_to "Add bookmark", new_list_bookmark_path(@list) %>
